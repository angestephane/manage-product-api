import { createUser } from "../user";

describe("user handler Unit test", () => {
  it("should successfully generate token and add user in db if username and password are correct", async () => {
    expect(200).toEqual(200);
  });
});
